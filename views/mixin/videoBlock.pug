mixin videoBlock(video = {})
    .videoBlock
        video.videoBlock__thumbnail(src=video.videoFile, autoplay=true, controls=true)
        h4.videoBlock_title=video.title
        h6.videoBlock__views=video.views